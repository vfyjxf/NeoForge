--- a/net/minecraft/client/gui/screens/ChatScreen.java
+++ b/net/minecraft/client/gui/screens/ChatScreen.java
@@ -41,6 +_,8 @@
     private ChatComponent.DisplayMode displayMode = ChatComponent.DisplayMode.FOREGROUND;
     protected ChatScreen.ExitReason exitReason = ChatScreen.ExitReason.INTERRUPTED;
     private CommandSuggestions commandSuggestions;
+    /// Neo: The areas the chat input box occupies on screen during the last frame, in GUI-scaled screen coordinates.
+    private final net.neoforged.neoforge.client.gui.RecordedScreenAreas renderedAreas = new net.neoforged.neoforge.client.gui.RecordedScreenAreas();
 
     public ChatScreen(String initial, boolean isDraft) {
         this(initial, isDraft, true);
@@ -141,6 +_,8 @@
             this.handleChatInput(this.input.getValue(), true);
             if (this.closeOnSubmit) {
                 this.exitReason = ChatScreen.ExitReason.DONE;
+                // Neo: Prevent closing the screen if another screen has been opened.
+                if (this.minecraft.gui.screen() == this)
                 this.minecraft.gui.setScreen(null);
             } else {
                 this.input.setValue("");
@@ -273,9 +_,29 @@
         return this.isDraft ? FormattedCharSequence.forward(text, Style.EMPTY.withColor(ChatFormatting.GRAY).withItalic(true)) : null;
     }
 
+    /// Neo: {@return the areas the chat input box occupies on screen during the last frame, in GUI-scaled screen coordinates}
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public java.util.List<net.minecraft.client.gui.navigation.ScreenRectangle> chatInputAreas() {
+        return this.renderedAreas.areas();
+    }
+
+    /// Neo: {@return the areas the chat history drawn by this screen occupies during the last frame, in GUI-scaled screen coordinates}
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public java.util.List<net.minecraft.client.gui.navigation.ScreenRectangle> chatHistoryAreas() {
+        return this.minecraft.gui.hud.getChat().renderedAreas();
+    }
+
+    /// Neo: {@return the areas the command suggestions occupy on screen during the last frame, in GUI-scaled screen coordinates}
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public java.util.List<net.minecraft.client.gui.navigation.ScreenRectangle> commandSuggestionAreas() {
+        return this.commandSuggestions.recordedAreas();
+    }
+
     @Override
     public void extractRenderState(GuiGraphicsExtractor graphics, int mouseX, int mouseY, float a) {
+        this.renderedAreas.clear();
         graphics.fill(2, this.height - 14, this.width - 2, this.height - 2, this.minecraft.options.getBackgroundColor(Integer.MIN_VALUE));
+        this.renderedAreas.record(new net.minecraft.client.gui.navigation.ScreenRectangle(2, this.height - 14, this.width - 4, 12));
         this.minecraft
             .gui
             .hud
