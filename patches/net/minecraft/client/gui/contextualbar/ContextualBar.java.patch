--- a/net/minecraft/client/gui/contextualbar/ContextualBar.java
+++ b/net/minecraft/client/gui/contextualbar/ContextualBar.java
@@ -35,6 +_,16 @@
 
     void extractRenderState(final GuiGraphicsExtractor graphics, final DeltaTracker deltaTracker);
 
+    /// Neo: {@return the areas occupied by this bar rendered during the last frame, in GUI-scaled screen coordinates}
+    @org.jetbrains.annotations.ApiStatus.Internal
+    default java.util.List<net.minecraft.client.gui.navigation.ScreenRectangle> recordedAreas() {
+        return java.util.List.of();
+    }
+
+    /// Neo: Clears the areas recorded for the previous frame. The HUD calls this at the start of every frame.
+    @org.jetbrains.annotations.ApiStatus.Internal
+    default void clearRecordedAreas() {}
+
     static void extractExperienceLevel(GuiGraphicsExtractor graphics, Font font, int experienceLevel) {
         Component str = Component.translatable("gui.experience.level", experienceLevel);
         int x = (graphics.guiWidth() - font.width(str)) / 2;
