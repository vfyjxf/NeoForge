--- a/net/minecraft/client/gui/components/SubtitleOverlay.java
+++ b/net/minecraft/client/gui/components/SubtitleOverlay.java
@@ -28,12 +_,17 @@
     private final List<SubtitleOverlay.Subtitle> subtitles = Lists.newArrayList();
     private boolean isListening;
     private final List<SubtitleOverlay.Subtitle> audibleSubtitles = new ArrayList<>();
+    /// Neo: The areas occupied by the subtitles rendered during the last frame, in GUI-scaled screen coordinates.
+    private final net.neoforged.neoforge.client.gui.RecordedScreenAreas recordedAreas = new net.neoforged.neoforge.client.gui.RecordedScreenAreas();
 
     public SubtitleOverlay(Minecraft minecraft) {
         this.minecraft = minecraft;
     }
 
     public void extractRenderState(GuiGraphicsExtractor graphics) {
+        // Neo: Discard the areas recorded during the last frame before the early returns below, so that
+        // subtitles that stopped rendering do not keep their areas.
+        this.recordedAreas.clear();
         SoundManager soundManager = this.minecraft.getSoundManager();
         if (!this.isListening && this.minecraft.options.showSubtitles().get()) {
             soundManager.addListener(this);
@@ -90,6 +_,8 @@
                         graphics.pose().translate(graphics.guiWidth() - halfWidth * 1.0F - 2.0F, graphics.guiHeight() - 35 - row * (height + 1) * 1.0F);
                         graphics.pose().scale(1.0F, 1.0F);
                         graphics.fill(-halfWidth - 1, -halfHeight - 1, halfWidth + 1, halfHeight + 1, this.minecraft.options.getBackgroundColor(0.8F));
+                        // Neo: Record the area the subtitle entry is drawn in, including its background
+                        this.recordedAreas.record(new net.minecraft.client.gui.navigation.ScreenRectangle(-halfWidth - 1, -halfHeight - 1, 2 * halfWidth + 2, 2 * halfHeight + 2).transformAxisAligned(graphics.pose()));
                         int textColor = ARGB.color(255, brightness, brightness, brightness);
                         if (!inView) {
                             if (rightness > 0.0) {
@@ -106,6 +_,12 @@
                 }
             }
         }
+    }
+
+    /// Neo: {@return the areas occupied by the subtitles rendered by this overlay during the last frame, in GUI-scaled screen coordinates}
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public java.util.List<net.minecraft.client.gui.navigation.ScreenRectangle> recordedAreas() {
+        return this.recordedAreas.areas();
     }
 
     @Override
