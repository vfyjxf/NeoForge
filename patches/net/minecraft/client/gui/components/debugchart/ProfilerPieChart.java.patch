--- a/net/minecraft/client/gui/components/debugchart/ProfilerPieChart.java
+++ b/net/minecraft/client/gui/components/debugchart/ProfilerPieChart.java
@@ -25,6 +_,8 @@
     private @Nullable ProfileResults profilerPieChartResults;
     private String profilerTreePath = "root";
     private int bottomOffset = 0;
+    /// Neo: The areas the pie chart drew during the last frame, in GUI-scaled screen coordinates.
+    private final net.neoforged.neoforge.client.gui.RecordedScreenAreas recordedAreas = new net.neoforged.neoforge.client.gui.RecordedScreenAreas();
 
     public ProfilerPieChart(Font font) {
         this.font = font;
@@ -38,7 +_,20 @@
         this.bottomOffset = bottomOffset;
     }
 
+    /// Neo: {@return the areas the pie chart drew during the last frame, in GUI-scaled screen coordinates}
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public java.util.List<net.minecraft.client.gui.navigation.ScreenRectangle> recordedAreas() {
+        return this.recordedAreas.areas();
+    }
+
+    /// Neo: Discards the areas recorded during the last frame, for example while the pie chart is not drawn.
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public void clearRecordedAreas() {
+        this.recordedAreas.clear();
+    }
+
     public void extractRenderState(GuiGraphicsExtractor graphics, int scaledScreenWidth, int scaledScreenHeight) {
+        this.recordedAreas.clear();
         if (this.profilerPieChartResults != null) {
             List<ResultField> list = this.profilerPieChartResults.getTimes(this.profilerTreePath);
             ResultField currentNode = list.removeFirst();
@@ -58,6 +_,11 @@
             List<String> currentNodeNameLines = this.splitNodeName(currentNodeName, topTextMaxWidth, topTextMaxWidth - 10);
             int currentNodeNameTop = chartCenterY - 62 - (currentNodeNameLines.size() - 1) * 9;
             graphics.fill(left - 5, currentNodeNameTop - 5, right + 5, bottom + 5, -1873784752);
+            // Neo: Record the area of the panel the pie chart is drawn in
+            this.recordedAreas.record(
+                new net.minecraft.client.gui.navigation.ScreenRectangle(left - 5, currentNodeNameTop - 5, right - left + 10, bottom - currentNodeNameTop + 10)
+                    .transformAxisAligned(graphics.pose())
+            );
             graphics.profilerChart(list, left, chartCenterY - 62 + 10, right, chartCenterY + 62);
             String firstLineText = "";
             if (!"unspecified".equals(currentNodeName) && !"root".equals(currentNodeName)) {
