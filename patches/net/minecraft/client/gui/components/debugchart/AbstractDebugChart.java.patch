--- a/net/minecraft/client/gui/components/debugchart/AbstractDebugChart.java
+++ b/net/minecraft/client/gui/components/debugchart/AbstractDebugChart.java
@@ -14,6 +_,8 @@
     protected static final int LINE_WIDTH = 1;
     protected final Font font;
     protected final SampleStorage sampleStorage;
+    /// Neo: The areas the chart drew during the last frame, in GUI-scaled screen coordinates.
+    private final net.neoforged.neoforge.client.gui.RecordedScreenAreas recordedAreas = new net.neoforged.neoforge.client.gui.RecordedScreenAreas();
 
     protected AbstractDebugChart(Font font, SampleStorage sampleStorage) {
         this.font = font;
@@ -28,7 +_,25 @@
         return 60 + 9;
     }
 
+    /// Neo: {@return the areas the chart drew during the last frame, in GUI-scaled screen coordinates}
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public java.util.List<net.minecraft.client.gui.navigation.ScreenRectangle> recordedAreas() {
+        return this.recordedAreas.areas();
+    }
+
+    /// Neo: Discards the areas recorded during the last frame, for example while the chart is not drawn.
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public void clearRecordedAreas() {
+        this.recordedAreas.clear();
+    }
+
     public void extractRenderState(GuiGraphicsExtractor graphics, int left, int width, int bottom) {
+        this.recordedAreas.clear();
+        // Neo: Record the area the chart and its labels occupy
+        this.recordedAreas.record(
+            new net.minecraft.client.gui.navigation.ScreenRectangle(left, bottom - this.getFullHeight(), width, this.getFullHeight())
+                .transformAxisAligned(graphics.pose())
+        );
         graphics.fill(left, bottom - 60, left + width, bottom, -1873784752);
         long avg = 0L;
         long min = 2147483647L;
