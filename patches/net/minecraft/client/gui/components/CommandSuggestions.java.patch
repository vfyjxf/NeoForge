--- a/net/minecraft/client/gui/components/CommandSuggestions.java
+++ b/net/minecraft/client/gui/components/CommandSuggestions.java
@@ -90,6 +_,8 @@
     private boolean allowHiding = true;
     private boolean messagesAllowed = true;
     private boolean commandsAllowed = true;
+    /// Neo: The areas the suggestions and the usage hint occupied on screen during the last frame, in GUI-scaled screen coordinates.
+    private final java.util.List<net.minecraft.client.gui.navigation.ScreenRectangle> recordedAreas = new java.util.ArrayList<>();
 
     public CommandSuggestions(
         Minecraft minecraft,
@@ -444,7 +_,15 @@
         return FormattedCharSequence.composite(parts);
     }
 
+    /// Neo: {@return the areas the suggestions and the usage hint occupied on screen during the last frame, in GUI-scaled screen coordinates}
+    @org.jetbrains.annotations.ApiStatus.Internal
+    public java.util.List<net.minecraft.client.gui.navigation.ScreenRectangle> recordedAreas() {
+        return java.util.List.copyOf(this.recordedAreas);
+    }
+
     public void extractRenderState(GuiGraphicsExtractor graphics, int mouseX, int mouseY) {
+        // Neo: Re-record the areas drawn this frame
+        this.recordedAreas.clear();
         if (!this.extractSuggestions(graphics, mouseX, mouseY)) {
             this.extractUsage(graphics);
         }
@@ -465,6 +_,14 @@
         for (FormattedCharSequence line : this.commandUsage) {
             int lineY = this.anchorToBottom ? this.screen.height - 27 - 12 * y : 72 + 12 * y;
             graphics.fill(this.commandUsagePosition - 1, lineY, this.commandUsagePosition + this.commandUsageWidth + 1, lineY + 12, this.fillColor);
+            // Neo: Record the area of the usage hint line drawn above
+            this.recordedAreas
+                .add(
+                    new net.minecraft.client.gui.navigation.ScreenRectangle(
+                        this.commandUsagePosition - 1, lineY, this.commandUsageWidth + 2, 12
+                    )
+                        .transformAxisAligned(graphics.pose())
+                );
             graphics.text(this.font, line, this.commandUsagePosition, lineY + 2, -1);
             y++;
         }
@@ -501,6 +_,12 @@
         }
 
         public void extractRenderState(GuiGraphicsExtractor graphics, int mouseX, int mouseY) {
+            // Neo: Record the area the suggestions are drawn in
+            CommandSuggestions.this.recordedAreas
+                .add(
+                    new net.minecraft.client.gui.navigation.ScreenRectangle(this.rect.getX(), this.rect.getY(), this.rect.getWidth(), this.rect.getHeight())
+                        .transformAxisAligned(graphics.pose())
+                );
             int limit = Math.min(this.suggestionList.size(), CommandSuggestions.this.suggestionLineLimit);
             int unselectedColor = -5592406;
             boolean hasPrevious = this.offset > 0;
